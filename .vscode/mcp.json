{
  "servers": {
    "stripe": {
      "command": "npx",
      "args": [
        "-y",
        "@stripe/agent-toolkit@latest",
        "mcp",
        "--api-key",
        "${env:STRIPE_SECRET_KEY}"
      ]
    }
  }
}
